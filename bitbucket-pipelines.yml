image: mcr.microsoft.com/playwright:v1.58.0 

pipelines:
  branches:
    develop:
      - step:
          name: Run Playwright Tests (Develop)
          caches:
            - node
          script:
            # Install Node dependencies
            - npm install

            # Run Playwright tests
            - npm run uitest
