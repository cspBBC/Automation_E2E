image: node:20

pipelines:
  branches:
    develop:
      - step:
          name: Run Playwright Tests (Develop)
          caches:
            - node
          script:
            # Install dependencies
            - npm install
            
            # Install Playwright browsers
            - npx playwright install
            
            # Run UI / other Playwright tests
            - npm run uitest
