# name: E2E Tests CI

# on:
#   push:
#     branches:
#       - main
#       - develop
#   pull_request:
#     branches:
#       - main
#       - develop

# jobs:
#   test:
#     runs-on: windows-latest
#     timeout-minutes: 30

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '18'
#           cache: 'npm'

#       - name: Install dependencies
#         run: npm ci

#       - name: 🔐 Configure environment
#         run: |
#           # Copy example .env.systest (you should store actual secrets in GitHub Secrets)
#           Copy-Item .env.example .env.systest -Force
#         shell: powershell

#       - name: 🧪 Run Full Test Suite (API + UI)
#         run: npm run test:ci
#         env:
#           ENVIRONMENT: systest
#           CI: true

#       - name: 📊 Upload test results
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: playwright-report
#           path: playwright-report/
#           retention-days: 30

#       - name: 📈 Publish test report
#         if: always()
#         uses: daun/playwright-report-action@v3
#         with:
#           report_path: playwright-report
